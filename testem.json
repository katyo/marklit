{
  "framework": "custom",
  "test_page": "testem.html",
  "src_files": [
    "src/**/*.ts",
    "src/**/*.coffee",
    "test/**/*.ts"
  ],
  "serve_files": [
    "test-out.js"
  ],
  "before_tests": "npm run compile-test",
  "on_exit": "npm run clean-test",
  "launchers": {
    "nodejs": {
      "command": "TS_NODE_TYPE_CHECK=y mocha -r ts-node/register test/index.ts -R tap",
      "protocol": "tap"
    }
  },
  "browser_args": {
    "chromium": [
      "--no-sandbox",
      "--disable-setuid-sandbox"
    ]
  },
  "launch_in_ci": [ "firefox", "chromium", "nodejs" ],
  "ignore_missing_launchers": true,
  "launch_in_dev": [ "firefox", "chromium", "safari", "nodejs" ]
}
